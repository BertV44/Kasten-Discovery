module github.com/BertV44/Kasten-Discovery

// Dependency-free for now: `go build ./...` and `go test ./...` work offline,
// with no go.sum and nothing to vendor. client-go arrives with the collector.
go 1.23
